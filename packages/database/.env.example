# Database Configuration
# This URL is used by Prisma for migrations and by the NestJS backend `pg.Pool` adapter for executing queries.

# For Local Development (Docker Compose)
# DATABASE_URL="postgresql://postgres:password@localhost:5432/who_know"

# For Supabase Production / Staging
# Go to your Supabase Project Settings -> Database -> Connection String -> URI
# Use the default "Session" connection pooler on port 5432. 
# Do NOT select "Transaction" pooling (port 6543) because the NestJS API manages its own pool using `pg.Pool`.
# Format: postgresql://[db-user].[project-ref]:[db-password]@aws-0-[region].pooler.supabase.com:5432/[db-name]
DATABASE_URL="postgresql://postgres:password@localhost:5432/who_know"
